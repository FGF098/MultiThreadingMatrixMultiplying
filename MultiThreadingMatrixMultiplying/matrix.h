#pragma once

#include "position.h"

#include <vector>
using namespace std;

/*
template<typename Value>

class MatrixMinor;

template<typename Value>

class Matrix
{
public:

	//Matrix(Size matrixSize);

	//Matrix(Size matrixSize, bool randomize);

	const Size size;
	const int pow2size;

	void setValue(Value& value, int i, int j);

	Matrix operator* (const Matrix& second) const;

	static Matrix StrassenMultiplying(const Matrix& first, const Matrix& second);

	virtual void rand() = 0;

private:

	//Matrix(Size matrixSize, int pow2Size);

	static void StrassenMultiplying(const MatrixMinor<Value>& first, const MatrixMinor<Value>& second, const MatrixMinor<Value>& result);

	static void StrasenMultiplyingSplitted(const MatrixMinor<Value>& first, const MatrixMinor<Value>& second, const MatrixMinor<Value>& result);

	int twoSize;

	vector<vector<Value*>> value;

};
*/