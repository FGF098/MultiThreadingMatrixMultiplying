#pragma once

#include "matrix.h"
#include "position.h"

/*
template<typename Value>

class MatrixMinor
{
public:

	MatrixMinor(const Matrix<Value>& current);
	MatrixMinor(const MatrixMinor<Value>& parent, Position position);

	const Matrix<Value>& base;

	int iDiff() const { return iTill - iFrom; };
	int jDiff() const { return jTill - jFrom; };

	const int iFrom;
	const int iTill;
	const int jFrom;
	const int jTill;

	//static void addition(const MatrixMinor& first, const MatrixMinor& second, const MatrixMinor& result);
	//static void division(const MatrixMinor& first, const MatrixMinor& second, const MatrixMinor& result);
};
*/